# Google OAuth Web Client ID (from Google Cloud Console → Clients)
# Copy this file to `.env` and replace the placeholder.
# Must match backend GOOGLE_CLIENT_ID on the server.
REACT_APP_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE
